services:
  backend:
    build: ./backend
    container_name: doctask-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: doctask-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
